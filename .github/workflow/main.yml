name: 'Workflow test'

on:
  push:
    branches:
      - main


jobs:
    test_job:
        runs-on: ubuntu-latest
        steps:
          - name: Run a one-line script
            run: echo Hello, world!